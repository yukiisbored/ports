$OpenBSD$

Index: Telegram/cmake/lib_tgvoip.cmake
--- Telegram/cmake/lib_tgvoip.cmake.orig
+++ Telegram/cmake/lib_tgvoip.cmake
@@ -826,15 +826,15 @@ if (NOT TGVOIP_FOUND)
     )
 
     if (LINUX)
-        find_package(PkgConfig REQUIRED)
-        find_package(ALSA REQUIRED)
-        pkg_check_modules(PULSE REQUIRED libpulse)
+        #find_package(PkgConfig REQUIRED)
+        #find_package(ALSA REQUIRED)
+        #pkg_check_modules(PULSE REQUIRED libpulse)
 
-        target_include_directories(lib_tgvoip_bundled
-        PRIVATE
-            ${ALSA_INCLUDE_DIRS}
-            ${PULSE_INCLUDE_DIRS}
-        )
+        #target_include_directories(lib_tgvoip_bundled
+        #PRIVATE
+        #    ${ALSA_INCLUDE_DIRS}
+        #    ${PULSE_INCLUDE_DIRS}
+        #)
 
         target_link_libraries(lib_tgvoip_bundled
         PRIVATE
